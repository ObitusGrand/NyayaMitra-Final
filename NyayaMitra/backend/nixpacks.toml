# Nixpacks configuration for Railway deployment
[phases.setup]
  nixPkgs = ["python311"]

[phases.install]
  cmds = ["pip install -r requirements.txt"]

[phases.build]
  cmds = ["python -m rag.load_statutes"]

[start]
  cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1"
